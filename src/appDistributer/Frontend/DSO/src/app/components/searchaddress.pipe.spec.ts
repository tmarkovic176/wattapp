import { SearchaddressPipe } from './searchaddress.pipe';

describe('SearchaddressPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchaddressPipe();
    expect(pipe).toBeTruthy();
  });
});
