import { SearchemployeelastnamePipe } from './searchemployeelastname.pipe';

describe('SearchemployeelastnamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchemployeelastnamePipe();
    expect(pipe).toBeTruthy();
  });
});
