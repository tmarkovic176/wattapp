import { SearchemployeenamePipe } from './searchemployeename.pipe';

describe('SearchemployeenamePipe', () => {
  it('create an instance', () => {
    const pipe = new SearchemployeenamePipe();
    expect(pipe).toBeTruthy();
  });
});
